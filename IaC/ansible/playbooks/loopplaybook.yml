
---
  - name: Loops PlayBook learn
    hosts: Server_VVM3
    become: yes

    tasks:
    - name: Say hello to ALL
      debug: msg="Hello {{ item }}"
      loop:
          - "User1"
          - "User2"
          - "User3"

    - name: Loop Until example
      shell: echo -n Z >> myfile.txt && cat myfile.txt
      register: output
      delay: 2
      retries: 10
      until: output.stdout.find("ZZZZ") == false

    - name: Print final output
      debug:
        var: output.stdout     
...
